name: Build DuduClock

permissions:
  contents: write

on:
  push:
    tags:
      - 'v*'
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Install arduino-cli
        run: |
          mkdir -p "$HOME/.local/bin"
          curl -fsSL https://raw.githubusercontent.com/arduino/arduino-cli/master/install.sh | BINDIR=$HOME/.local/bin sh
          echo "$HOME/.local/bin" >> $GITHUB_PATH

      - name: Install ESP32 core
        run: |
          arduino-cli config init --overwrite
          arduino-cli config add board_manager.additional_urls https://raw.githubusercontent.com/espressif/arduino-esp32/gh-pages/package_esp32_index.json
          arduino-cli core update-index
          arduino-cli core install esp32:esp32@2.0.9

      - name: Install Arduino libraries
        run: |
          arduino-cli lib install "OneButton@2.6.2"
          arduino-cli lib install "TFT_eSPI@2.5.43"
          arduino-cli lib install "ArduinoJson@7.4.3"
          arduino-cli lib install "TaskScheduler@4.0.8"

      - name: Compile firmware
        run: |
          arduino-cli compile \
            --fqbn esp32:esp32:AirM2M_CORE_ESP32C3:UploadSpeed=921600,CDCOnBoot=default,CPUFreq=80,FlashFreq=80,PartitionScheme=huge_app,DebugLevel=none,EraseFlash=all \
            --libraries ./libraries \
            --build-path build \
            .

      - name: Package flash bins
        run: |
          mkdir -p release-bin
          for file in build/*.bin; do
            name="$(basename "$file")"
            cp "$file" "release-bin/AirM2M_CORE_ESP32C3_240x320_$name"
          done

      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            release-bin/*.bin
          body: |
            ## 刷机说明
            本次发布只保留可刷机的 `.bin` 文件。
            `AirM2M_CORE_ESP32C3` 就是合宙 ESP32-C3 开发板。

            下载后请保留以下文件：
            - `AirM2M_CORE_ESP32C3_240x320_DuduClock.ino.bin`
            - `AirM2M_CORE_ESP32C3_240x320_DuduClock.ino.bootloader.bin`
            - `AirM2M_CORE_ESP32C3_240x320_DuduClock.ino.partitions.bin`

            推荐烧录方式：
            1. 连接开发板到电脑。
            2. 使用仓库里的 `build-flash-monitor.ps1` 或你常用的 ESP32 烧录工具。
            3. 选择正确串口后直接刷入上述 `.bin` 文件。

            这个 Release 里不再提供 `.elf` 和 `.map`，它们只用于调试，不参与刷机。

      - name: Upload firmware artifact
        uses: actions/upload-artifact@v4
        with:
          name: DuduClock-firmware
          path: |
            release-bin/*.bin
          retention-days: 7
